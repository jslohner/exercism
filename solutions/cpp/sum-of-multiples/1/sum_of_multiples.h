#pragma once
#include <iostream>
#include <numeric>
#include <set>
#include <vector>

using namespace std;

using item_list = vector<int>;

namespace sum_of_multiples {
    long to(item_list items, long level);
}  // namespace sum_of_multiples
